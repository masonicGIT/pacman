# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3001

# Allowed CORS origin (set to your frontend URL in production)
ALLOWED_ORIGIN=*

# ─── Security ─────────────────────────────────────────────────────────────────
# Secret used to sign session JWTs — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# Admin API key for triggering payouts — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_KEY=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# ─── House Wallet Addresses ───────────────────────────────────────────────────
# The addresses where players send their 25¢ entry fees
HOUSE_WALLET_SOLANA=SOLANA_WALLET_ADDRESS_HERE
HOUSE_WALLET_BASE=BASE_WALLET_ADDRESS_HERE

# ─── House Private Keys (optional — for automated payouts) ────────────────────
# WARNING: If configured, this server can send funds from these wallets.
# Only set these in a secure, isolated environment. Never commit to git.
#
# Solana: JSON array of secret key bytes, e.g. from Phantom → Export Private Key
# HOUSE_PRIVATE_KEY_SOLANA=[1,2,3,...]
#
# Base: hex-encoded private key (0x...)
# HOUSE_PRIVATE_KEY_BASE=0x...

# ─── RPC Endpoints ────────────────────────────────────────────────────────────
# Public endpoints are used by default. Replace with paid endpoints for production.
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
BASE_RPC_URL=https://mainnet.base.org
